<%= provide(:title, "Dicas") %>
      <!-- Page Heading -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Dicas</h1>
            </div>
        </div>
        <!-- /.row -->
        <% @tips.each do |tip| %>
        <!-- Project One -->
        <div class="row">
            <div class="col-md-7">
              <a href="#">
                <%= image_tag("http://placehold.it/700x300", :alt => "rss feed") %>
              </a>
            </div>
            <div class="col-md-5">
                <h3><%= tip.title %></h3>
                <h4>Categoria: <%= tip.category.title %></h4>
                <p><%= tip.description %></p>
                <%= link_to 'Ver Mais', tip %>
            </div>
        </div>
        <!-- /.row -->
        <% end %>
